import 'package:flutter/material.dart';

class CreateRequireScreen extends StatelessWidget {
  const CreateRequireScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('haha'),
    );
  }
}
