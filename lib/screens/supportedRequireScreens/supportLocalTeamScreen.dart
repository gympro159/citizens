import 'package:citizens/widgets/appBar/appBar.dart';
import 'package:flutter/material.dart';

class SupportLocalTeamScreen extends StatelessWidget {
  const SupportLocalTeamScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('huhu'),
    );
  }
}
